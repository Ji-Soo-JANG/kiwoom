# Kiwoom 프로젝트 작업 목록

마지막 정리: 2026-08-22

> 자동매매 목표의 상세 단계와 통과 기준은 `docs/auto-trading-roadmap.md`를 기준으로 한다.

> 대상은 KRX 국내 주식 현물 보통주의 현금 매수·보유 수량 내 매도만이다. 선물·옵션·공매도·신용·미수·해외 주식·가상자산은 범위에서 제외한다.

## 자동매매 로드맵 — 다음 작업

### 전략 연구 플랫폼

- [x] 공통 `StockStrategy` 인터페이스와 버전 기반 전략 레지스트리를 구현한다.
- [x] 기존 급락-횡보-돌파-눌림 v1 탐지기를 독립 전략 구현체로 이전한다.
- [x] 전략 설명·상태·파라미터를 DB에 버전별 불변 정의로 저장한다.
- [x] 전략 목록 API와 자동매매 화면의 전략·파라미터 조회를 제공한다.
- [ ] 거래량 선행 축적 전략을 추가하고 기존 v1과 동일 데이터에서 비교한다.
- [ ] 변동성 압축 후 확장 전략을 추가한다.
- [ ] 전략별 백테스트·워크포워드·PAPER 성과 비교와 승격 상태 전이를 구현한다.

### P0 — 연구 기반 확립

- [x] 전략 버전과 후보 신호 스냅샷을 DB에 저장한다.
  - 스캔 실행 ID, 전략 버전, 박스권 기간, 데이터 기준일과 전체 종목의 입력 특징·판정 결과를 저장하고 최신 실행 조회 API를 제공한다.
- [x] 일봉 데이터 품질 검사와 수정주가·기업행사 처리 정책을 구현한다.
  - `upd_stkpc_tp=1` 수정주가 원본을 보존하고 OHLC·거래량·누락 거래일·35% 초과 가격 단절을 검사한다.
  - 최근 검사에서 차단 문제가 발견된 종목은 기업행사 확인 전까지 전략 후보에서 제외한다.
- [x] 시점 기준 종목 마스터로 생존편향 없는 연구 데이터셋을 만든다.
  - 정상적으로 새로 받은 전체 종목 목록을 날짜별 스냅샷으로 보존한다.
  - 전략 조회의 `asOf` 기준일에는 그날까지 확인된 종목 구성과 일봉만 사용한다.
  - 최초 스냅샷 수집 이전에 이미 상장폐지된 종목은 복원할 수 없으므로 연구 범위에서 명시적으로 제외한다.
- [x] 수수료·세금·슬리피지를 포함한 이벤트 기반 백테스트 엔진을 구현한다.
  - 신호 다음 거래일 시가에 진입하고 손절·익절·최대 보유기간·테스트 종료 이벤트로 청산한다.
  - 동일 일봉에서 손절과 익절이 모두 충족되면 손절을 먼저 적용하는 보수적 체결 순서를 사용한다.
  - 양방향 수수료·슬리피지와 매도세를 반영한 거래별 순손익 및 전체 성과를 DB에 저장한다.
- [x] 워크포워드 검증과 비용 후 기대값·최대 낙폭 보고서를 제공한다.
  - 거래일 기준 학습·검증 구간을 완전히 분리하고 검증 창을 앞으로 이동해 반복 평가한다.
  - 검증 구간의 비용 후 기대값·평균 수익률·최대 낙폭과 비용 0원 대비 감소액을 보고한다.
  - 최소 2개 검증 구간·5건 거래·양의 기대값·최대 낙폭 -20% 이내를 기본 연구 통과 기준으로 기록한다.

### P0 — 매매 안전장치

- [x] 실주문과 분리된 `SIGNAL_ONLY`·`PAPER`·`LIVE` 실행 모드를 만든다.
  - 기본값은 `SIGNAL_ONLY`이며 `PAPER`만 로컬 체결을 허용한다.
  - `LIVE`는 환경변수와 확인 문구가 있어도 브로커 주문 어댑터가 없으면 `SIGNAL_ONLY`로 강등한다.
- [x] 중복 주문을 막는 주문 상태 머신과 체결·잔고 대사를 구현한다.
  - 고유 `decisionId`를 DB에서 유일하게 보장하고 동일 요청은 기존 주문을 반환한다.
  - 주문 상태 전이와 체결을 기록하고 PAPER 주문·체결·잔고·현금의 일관성을 다시 계산한다.
- [x] 종목·계좌·일일 손실 한도와 수동 재개형 킬 스위치를 구현한다.
  - 종목 비중 10%, 총 투자 50%, 일일 손실 2%, 누적 낙폭 10%, 최대 5종목을 기본 한도로 둔다.
  - 한도 초과 시 신규 매수와 진행 주문을 차단하며 정확한 확인 문구를 사용한 수동 재개만 허용한다.

### P1 — 장중 검증

- [x] 후보 종목의 분봉·실시간 시세를 기록하고 재생하는 기능을 구현한다.
  - 원본 이벤트를 고유 ID로 중복 없이 저장하고, 최대 7일 범위를 시간순으로 재생한다.
  - 동일 입력의 SHA-256 체크섬과 분봉 OHLCV 집계를 제공한다.
- [x] 20거래일 관찰 모드와 백테스트·실시간 신호 비교 리포트를 구현한다.
  - 날짜·종목별 신호를 멱등 저장하고 일치율, 누락·예상 밖 신호, 가격 편차를 계산한다.
  - 서로 다른 거래일이 20일 이상 누적되기 전에는 `complete=false`를 유지한다.
- [ ] 실제 장중 데이터로 최소 20거래일을 누적한 뒤 비교 결과를 판정한다.
  - 기능과 20일 경계 테스트는 완료했으며 실제 관찰 기간이 지나야 완료할 수 있다.
- [x] 외부 주문 없는 로컬 PAPER 시나리오로 부분 체결·미체결·정정·취소·재시작 복구·중복 체결 방지를 검증한다.
- [ ] 키움 모의투자 서버에서 부분 체결·미체결·정정·취소·장애 복구를 현장 검증한다.
  - 실제 모의 주문은 사용자 승인과 모의투자 자격증명이 준비된 별도 실행에서 진행한다.

### P2 — 제한적 실매매

- [x] 후보 알림과 사용자 수동 승인 주문 워크플로를 구현한다.
  - 후보를 만료 시각과 함께 저장하고 승인 화면에서 명시적인 확인을 거쳐 PAPER 주문만 생성한다.
  - 동일 신호·주문 의사결정 ID로 재시도해도 중복 주문을 만들지 않는다.
  - 최신 전략 스캔의 통과 종목을 가격 한도 안에서 승인 후보로 자동 등록한다.
- [x] 최소 규모의 제한 주문과 엄격한 손실 한도를 구현한다.
  - 주문당 최대 10만원, 최대 한 종목, 하루 2회로 고정하며 기존 계좌·손실·낙폭 한도와 킬 스위치를 함께 적용한다.
  - 주문 규모는 자동으로 확대하지 않는다.
- [x] 실현 슬리피지와 전략 성능 저하 감시 및 자동 주문 중단을 구현한다.
  - 5개 이상 표본에서 평균 절대 슬리피지 1% 초과 또는 비용 후 평균 수익률이 음수면 킬 스위치를 활성화한다.
  - 승인된 PAPER 진입 체결의 예상가·실제 체결가·슬리피지를 주문 ID 기준으로 자동 기록한다.
  - 승인 화면에서 외부 주문 없이 부분 체결·미체결·정정·취소·복구 시나리오를 반복 검증한다.
- [ ] 키움 모의투자에서 제한 주문을 충분히 검증한 뒤 실제 최소 금액 주문을 사용자 승인으로 현장 검증한다.
  - 현재 실주문 브로커 어댑터는 의도적으로 연결하지 않았으며 `LIVE`는 계속 `SIGNAL_ONLY`로 강등된다.

### P3 — 다음 코드 작업: 현물 청산과 거래 성과

- [x] PAPER 포지션의 손절가·익절가·최대 보유기간과 청산 상태를 저장한다.
- [x] 사용자 승인 기반 PAPER 매도와 보유 수량 초과·중복 청산 방지를 구현한다.
- [x] 장중 가격으로 손절·익절·보유기간 만료를 판정해 청산 승인 후보를 생성한다.
- [x] 매수·매도 체결, 수수료·세금을 연결해 비용 후 실현손익과 순수익률을 자동 기록한다.
- [x] 완료 거래의 승률·손익비·Profit Factor·연속 손실·최근 기대값·최대 낙폭을 계산한다.
- [x] 승인 화면을 진입 대기·보유·청산 대기·완료 거래로 분리한다.
  - 기본 청산 기준은 손절 -5%, 익절 +10%, 최대 보유 10일이며 손절을 우선 판정한다.
  - 실제 증권 주문은 전혀 전송하지 않고 기존 PAPER 주문·체결·잔고에만 반영한다.

### P4 — 관찰 자동화와 모의투자

- [x] 모의투자·실투자 자동매매 ON/OFF와 전략 선택 제어를 구현한다.
  - PAPER ON은 후보 진입과 조건부 청산을 자동 실행하며 기존 위험 한도·킬 스위치를 유지한다.
  - 실투자 ON은 준비 상태만 저장하고 브로커 어댑터가 없으므로 실제 주문 전송은 계속 차단한다.
- [x] 거래일 스케줄에 맞춰 데이터 갱신·전략 스캔·관찰 기록을 자동 실행한다.
  - 기본은 비활성화이며 `TRADING_SCHEDULER_ENABLED=true`일 때 거래일 09:05에 실행한다.
  - 장중 PAPER 스윙 포지션을 1분마다 감시하고 장후 대사·성과를 기록한다.
  - Windows 작업 스케줄러 등록·해제 스크립트로 평일 서버 시작과 종료를 자동화한다.
  - 최신 거래일까지 수집되지 않은 종목은 신규 전략 후보에서 제외한다.
- [x] 20거래일 진행률, 누락 신호, 신호 일치율과 가격 편차를 화면에 표시한다.
- [x] 현물 주문 전용 `BrokerAdapter`와 로컬 PAPER 구현을 분리한다.
- [x] 키움 모의투자 주문·정정·취소·부분 체결·잔고·거부·timeout 픽스처와 외부 주문 차단 테스트를 구현한다.
  - 개인정보를 제거한 예상 계약이며 실제 모의투자 서버 현장 검증 후 필드값을 확정한다.
- [ ] 실제 키움 모의투자에서 최소 수량 주문과 재시작 대사를 현장 검증한다.

## P0 — 실행 및 테스트 안정화

- [x] `CorsConfig`와 `WebClientConfig`에 중복 선언된 `corsWebFilter` 빈을 하나로 통합했다.
  - 완료 기준: Spring 애플리케이션 컨텍스트가 빈 이름 충돌 없이 시작된다.
- [x] `KiwoomApiServiceTest`를 외부 API와 환경변수에 의존하지 않는 단위 테스트로 전환했다.
  - MockWebServer 또는 WebClient mock을 사용한다.
  - null/빈 종목 코드, 정상 응답, HTTP 오류, 잘못된 JSON, API 오류 코드를 검증한다.
  - 실제 키움 API 호출 테스트는 별도의 통합 테스트로 분리하고 명시적으로 활성화할 때만 실행한다.
- [x] Windows의 `mvnw.cmd` 실행 오류를 수정했다.
  - 완료 기준: Windows에서 `.\mvnw.cmd clean test`가 실행된다.
- [x] 애플리케이션 기동 테스트를 추가했다.
  - 완료 기준: `@SpringBootTest` 또는 적절한 slice test로 주요 빈 구성이 검증된다.

## P1 — 키움 API 연동 안정성

- [x] 접근 토큰의 만료 시각을 관리하고 만료 전에 재발급하도록 개선했다.
  - 현재 `Mono.cache()`는 최초 토큰을 계속 재사용하므로 만료 후 요청 실패 가능성이 있다.
- [x] HTTP 401 인증 실패 시 토큰을 한 번 갱신한 뒤 요청을 재시도하도록 구현했다.
- [x] 429 및 일시적인 5xx 오류에 제한적인 재시도와 지수 백오프를 적용했다.
- [x] 연결/응답 timeout과 최대 연결 수를 `application.properties`에서 조정할 수 있도록 설정화했다.
- [x] 키움 HTTP·재시도는 `client`, JSON 변환은 `mapper`, 조회 흐름은 `service`로 분리했다.
- [x] API 전체 응답 본문 로그를 제거해 민감정보 및 과도한 데이터가 기록되지 않게 했다.

## P1 — API와 오류 처리

- [x] 전역 예외 처리기(`@RestControllerAdvice`)를 추가하고 오류 응답 형식을 통일했다.
  - 예: `code`, `message`, `timestamp`, `path`
- [x] 종목 코드를 6자리 숫자로 검증하고 다중 조회의 빈 값, 중복 값, 최대 개수를 제한했다.
- [x] `baseDate`가 실제 유효한 `yyyyMMdd` 날짜인지 검증했다.
- [x] 다중 종목 조회는 한 종목이라도 실패하면 전체 요청을 실패 처리하도록 정책과 테스트를 추가했다.
- [x] OpenAPI/Swagger 문서를 도입하고 README의 API 명세를 실제 구현과 동기화했다.
  - 단일 조회 실제 경로: `GET /api/kiwoom/stock-price/{code}`
  - 일봉 조회 경로: `GET /api/kiwoom/stock-price/{code}/daily`

## P1 — 설정 및 보안

- [x] 키움 설정을 `@ConfigurationProperties`로 묶고 시작 시 필수값을 검증한다.
- [x] 개발/테스트/운영 프로필을 분리했다.
- [x] CORS 허용 origin을 `CORS_ALLOWED_ORIGINS` 환경 설정으로 이동했다.
- [x] API 키와 secret이 소스, 로그, 정적 파일, 빌드 산출물에 포함되지 않는지 점검했다.
- [x] 취약점·업데이트 후보를 점검하고 Dependabot 및 프레임워크 업그레이드 정책을 추가했다.

## P2 — 프론트엔드 품질

- [x] API 요청의 로딩·빈 결과·네트워크 오류·잘못된 입력 상태를 접근 가능한 상태 메시지로 표시한다.
- [x] 종목 검색 폼, 결과 목록, 일봉 차트에 컴포넌트 테스트를 추가했다.
- [x] 프론트엔드 ESLint와 Vitest 명령을 추가하고 CI 검사에 연결했다.
- [x] Vite 빌드 결과가 Spring의 `static` 디렉터리에 배포되도록 설정했다.
- [x] 프론트엔드 차트 번들을 지연 로딩 및 별도 chunk로 분리했다.
- [x] 폼 키보드 제출, label·ARIA 상태, 포커스·색상 대비, 모션 감소 및 모바일 레이아웃을 점검했다.

## P2 — 기능 확장

- [x] 관심 종목(Watchlist)을 메모리에 저장하고 조회·삭제하는 기능을 구현했다.
- [x] 포트폴리오 매입가·수량을 관리하고 키움 현재가 기준 수익률을 계산한다.
- [x] RSI(14), MACD(12·26·9) 기술적 지표를 계산해 차트에 표시했다.
- [x] 현재가 조회에 설정 가능한 짧은 TTL 로컬 캐시를 적용해 중복 키움 API 호출을 줄였다.
  - 먼저 로컬 캐시 필요성을 측정하고, 다중 인스턴스가 필요할 때 Redis를 검토한다.

## P2 — 문서 및 자동화

- [x] README의 프로젝트 트리를 새 패키지 구조(`config`, `controller`, `dto`, `service`, `error`)로 갱신한다.
- [x] Java 21·Node.js 22 환경에서 클린 설치, 테스트, 빌드, JAR 기동 및 health 응답을 재검증했다.
- [x] 백엔드 테스트와 프론트엔드 빌드를 실행하는 GitHub Actions CI를 추가했다.
- [x] 운영 상태 확인용 Actuator health/info endpoint와 노출 범위를 문서화했다.

## 완료된 작업

- [x] Java 클래스를 역할별 패키지로 분리했다.
  - `config`: Spring/WebClient/CORS 설정
  - `controller`: REST 및 웹 진입점
  - `dto`: 현재가 및 일봉 응답 모델
  - `service`: 키움 API 연동 및 조회 로직
- [x] 이동된 클래스의 package/import와 서비스 테스트 패키지를 정리했다.
- [x] DTO getter를 명시적으로 선언해 현재 빌드 환경에서 메인 및 테스트 소스 컴파일을 확인했다.
- [x] MockWebServer 기반 테스트와 Spring 컨텍스트 기동 테스트를 추가했다.
- [x] 종목 코드·기준일자·다중 조회 개수 검증과 공통 API 오류 응답을 추가했다.

## 다음 로드맵

> 이 프로젝트는 로컬 실행·학습용으로 관리하며 서버 배포, HTTPS, 리버스 프록시,
> 다중 인스턴스와 운영 인프라는 작업 범위에서 제외한다.

### P0 — 데이터 영속화와 사용자 분리

- [x] PostgreSQL·R2DBC·Flyway를 도입해 관심 종목과 포트폴리오를 영구 저장한다.
  - Docker Compose, 환경변수 예시, H2 통합 테스트를 구성했다.
  - 실제 PostgreSQL에서 Flyway 적용과 애플리케이션 재시작 후 데이터 유지를 검증했다.
- [x] 매수·매도 거래 내역을 저장하고 이동평균 매입가를 자동 계산한다.
  - 매도 시 실현 손익을 저장하고 기존 현재가 평가에서는 미실현 손익을 제공한다.
  - 거래별 수수료와 세금을 반영하며 거래와 포지션 변경을 한 트랜잭션으로 처리한다.
- [x] Spring Security 기반 사용자 인증을 도입한다.
  - 백엔드·프론트 통합 배포에 맞춰 HttpOnly 세션 로그인을 적용했다.
  - 관심 종목, 포트폴리오, 거래 원장을 인증 사용자별로 격리했다.
  - Swagger와 관리 endpoint는 관리자 권한으로 제한했다.

### P1 — 로컬 통합 검증

- [x] 실제 키움 API 통합 테스트를 별도 프로필로 추가한다.
  - 명시적으로 활성화할 때만 실행하고 CI의 기본 테스트에서는 제외한다.
  - 모의투자와 운영 환경의 URL 및 자격증명을 분리한다.
  - `mvn verify -Pkiwoom-live`에서 선택 환경의 현재가·일봉 응답을 검증한다.

### P1 — API 안정성과 관측성

- [x] 일봉 조회에 종목·기준일 단위 TTL 캐시를 적용한다.
  - 현재가와 일봉의 캐시 TTL을 독립적으로 설정한다.
  - 캐시 적중률과 실제 키움 API 호출 횟수를 측정한다.
- [x] 키움 오류 코드를 애플리케이션 오류 코드로 세분화한다.
  - 인증 만료, 호출 제한, 종목 없음, 장 운영시간 오류를 구분한다.
  - 프론트엔드에 오류 유형별 안내와 재시도 동작을 제공한다.
- [x] Micrometer·Prometheus 기반 로컬 진단 지표를 추가한다.
  - API 응답 시간, 실패율, 401 갱신, 429 재시도, 캐시 적중률을 수집한다.
- [x] 요청 추적 ID와 구조화 JSON 로그를 도입한다.
  - API 키, secret, 접근 토큰이 로그에 포함되지 않는지 자동 검사한다.
- [x] 목표가와 기술적 지표 조건에 따른 알림 기능을 설계한다.
  - 로컬 프로젝트에 맞춰 앱 내부 알림을 1차 채널로 결정했다.
  - 상세 설계: `docs/alert-design.md`
- [x] 목표가 앱 알림 백엔드를 구현한다.
  - PostgreSQL에 사용자별 규칙과 이벤트를 저장하고 규칙 CRUD·읽음 처리를 제공한다.
  - 목표가 경계에 처음 진입할 때만 이벤트를 생성하고, 조건 이탈 후 재진입할 때 다시 알린다.
  - 원자적 상태 전환으로 동시 평가 시 중복 이벤트 생성을 방지한다.
- [x] 목표가 알림 설정·배지·이벤트 목록 UI를 구현한다.
  - 규칙 생성·활성화·삭제, 수동 평가, 미읽음 배지와 이벤트 읽음 처리를 제공한다.
- [x] RSI·MACD 계산을 백엔드로 이동하고 기술지표 알림 조건을 확장한다.
  - 일봉 응답에 RSI(14), MACD(12·26), Signal(9)을 포함한다.
  - RSI 이상·이하와 MACD 상향·하향 교차 규칙을 앱 알림과 설정 UI에 추가했다.

### P1 — 프론트엔드 기능과 사용성

- [x] React Router로 검색·관심 종목·포트폴리오·알림 화면을 분리한다.
  - 직접 URL 접근과 새로고침을 위한 Spring SPA 진입점 fallback을 제공한다.
  - 종목 검색 입력 화면과 조회 결과·차트 화면을 별도 탭으로 분리한다.
- [x] TanStack Query를 도입해 API 캐시, 재시도, 로딩 상태를 통합한다.
  - 관심 종목·포트폴리오·알림 조회를 캐시하고 mutation 성공 시 관련 데이터를 무효화한다.
  - 호출 제한·일시 장애·5xx만 최대 2회 재시도하며 입력·인증 오류는 재시도하지 않는다.
- [x] 종목명 검색과 자동완성 기능을 추가한다.
  - 키움 `ka10099` 코스피·코스닥 목록을 12시간 캐시하고 코드·종목명·초성 검색 API를 제공한다.
  - 시장 필터와 브라우저 로컬 최근 검색 5개를 제공한다.
  - 띄어쓰기 없는 종목명과 Enter 첫 결과 바로 조회를 지원한다.
  - 보통주·우선주·ETF·ETN·리츠·스팩 상품 필터와 상품유형 키워드 검색을 지원한다.
  - 검색 후보에 시장과 상품유형을 함께 표시한다.
- [x] 실시간 시장 순위 기반의 종목 발견 화면을 추가한다.
  - 급등주·급락주·거래량 상위 목록을 한 탭에서 동시에 제공한다.
  - 목록의 종목을 클릭하면 기존 현재가와 일봉 차트 조회 화면으로 이동한다.
  - 키움 순위 API 응답은 30초 캐시해 화면 갱신 시 호출량을 제한한다.
- [x] 포트폴리오 수정 UI와 전체 자산 요약을 추가한다.
  - 총 매입금액, 평가금액, 손익, 수익률, 종목별 비중을 표시한다.
- [x] 포트폴리오 화면을 키움 계좌 평가잔고 기반으로 전환한다.
  - `ka00001` 계좌번호와 `kt00018` 평가잔고를 연결하고 결과를 10초 캐시한다.
  - 수동 포지션 등록·수정·삭제 UI 대신 실제 보유종목과 계좌 요약을 표시한다.
- [x] 차트를 캔들스틱·거래량·이동평균선까지 확장한다.
  - RSI와 MACD는 별도 보조지표 패널로 분리한다.
  - 조회 기간 선택과 확대·축소를 지원한다.
- [x] 급등락 알림 조건과 설정 화면을 추가한다.
  - 목표가 설정 화면은 완료했으며, 급등락 조건은 `docs/alert-design.md` 모델을 확장한다.

### P2 — 테스트와 코드 품질

- [x] `KiwoomHttpClient`와 `KiwoomResponseMapper` 전용 단위 테스트를 추가한다.
  - 필드 누락, 숫자 형식 오류, timeout, 재시도 소진을 검증한다.
- [x] Playwright 기반 브라우저 E2E 테스트를 추가한다.
  - 검색, 관심 종목, 포트폴리오 등록·평가의 핵심 흐름을 검증한다.
  - 모바일 화면과 네트워크 오류 시나리오를 포함한다.
- [x] JaCoCo와 Vitest coverage를 CI에 연결하고 최소 커버리지 기준을 정한다.
- [x] OpenAPI 기반 API 계약 검증과 프론트 타입 생성을 도입한다.
  - `docs/openapi.json`과 Springdoc 런타임 경로를 계약 테스트로 대조한다.
  - `openapi-typescript` 생성 결과가 최신인지 CI에서 검사한다.
- [x] 사용하지 않는 Thymeleaf 의존성과 템플릿을 제거했다.
  - React 정적 진입점만 사용하므로 서버 템플릿 엔진을 유지하지 않는다.
- [x] Java 포맷터와 Prettier를 적용해 CI에서 코드 형식을 검사한다.
  - Maven `validate`에서 Spotless를, 프론트 CI에서 Prettier를 검사한다.
- [x] Vite 정적 빌드 산출물을 Git에서 직접 관리하기로 결정했다.
  - 로컬 JAR 단독 실행을 위해 산출물을 포함하고 CI 재빌드로 최신 여부를 확인한다.

## 후속 로드맵

> 아래 항목도 로컬 실행·학습용 범위에서 진행한다. 서버 배포와 운영 인프라는 계속 제외한다.

### P0 — 로그인과 개발 실행 경험

- [x] 개발자 도구 없이 사용할 수 있는 브라우저 로그인 화면을 추가한다.
  - `GET /login`에서 폼을 제공하고 인증 실패 메시지를 표시한다.
  - `dev` 프로필 로그인 성공 시 Vite 개발 서버(`http://localhost:5173`)로 이동한다.
- [x] React에서 인증 상태를 확인하고 401 응답 시 로그인 화면으로 안내한다.
  - 최초 화면에서 보호 API를 무조건 호출해 401 로그를 남기지 않게 한다.
  - 세션 만료 시 현재 경로를 보존한 채 로그인 후 돌아오게 한다.
- [x] 헤더에 로그인 사용자와 로그아웃 버튼을 추가한다.
  - 로그아웃 성공 시 Query 캐시를 비우고 로그인 화면으로 이동한다.
- [x] 로컬 개발 환경을 한 명령으로 시작하는 스크립트를 제공한다.
  - PostgreSQL health 확인, 백엔드와 Vite 실행, 종료 절차를 PowerShell로 자동화한다.
  - Docker Desktop 미실행, 포트 충돌, Java·Node 버전 오류를 사전에 안내한다.
- [x] IntelliJ 실행 설정 예시와 개발 서버 매뉴얼을 저장소 문서로 고정한다.
  - 키·시크릿을 노출하지 않는 환경변수 설정 예시를 포함한다.

### P1 — 로컬 설정과 데이터 관리

- [x] 시작 시 환경 설정 진단을 개선한다.
  - 활성 프로필, DB 접속 대상, 키움 모의/운영 환경을 값 노출 없이 요약한다.
  - placeholder 키, 기본 로그인 비밀번호, DB 비밀번호 불일치를 명확한 오류로 안내한다.
- [x] 로컬 DB 백업·복원·초기화 명령을 문서화하고 스크립트로 제공한다.
  - 관심 종목, 포트폴리오, 거래 및 알림 데이터를 안전하게 백업한다.
  - 데이터 삭제 명령은 명시적인 확인 옵션이 있을 때만 실행한다.
- [x] 거래 내역과 알림 이벤트에 페이지네이션을 적용한다.
  - 정렬 기준과 기본 페이지 크기를 API 계약에 반영한다.
  - 데이터가 많아져도 전체 행을 한 번에 읽지 않게 한다.
- [x] 관심 종목과 포트폴리오 입력 검증을 DTO 수준으로 통일한다.
  - 수량·가격·수수료·세금의 음수, 0, 과도한 소수 자릿수를 검증한다.
  - 컨트롤러와 서비스의 중복 검증을 제거한다.

### P1 — 자동 알림과 시장 데이터

- [x] 알림 평가를 수동 버튼뿐 아니라 로컬 스케줄러로 실행할 수 있게 한다.
  - 설정으로 활성화하며 장 운영시간과 API 호출 제한을 고려한다.
  - 동일 시각 중복 실행과 중복 이벤트 생성을 방지한다.
- [x] 휴장일과 장 운영시간 판정을 개선한다.
  - 주말뿐 아니라 한국거래소 휴장일을 로컬 캘린더로 관리한다.
  - 장외 시간에는 사용자에게 마지막 데이터 시각을 명확히 표시한다.
- [x] 키움 종목 목록 캐시를 수동 갱신하고 상태를 확인하는 관리자 기능을 추가한다.
  - 마지막 갱신 시각, 종목 수, 실패 원인을 화면에서 확인한다.
- [x] 일봉 데이터의 조회 기간과 최대 건수를 API 요청값으로 제공한다.
  - 프론트 기간 선택이 실제 백엔드·키움 조회 범위와 일치하도록 한다.

### P2 — 프론트엔드 구조와 품질

- [x] 생성된 OpenAPI 타입을 프론트 API 호출 코드에 실제로 연결한다.
  - JSDoc 또는 TypeScript 전환으로 요청·응답 타입을 편집기와 빌드에서 검사한다.
- [x] 로그인·로그아웃·세션 만료 흐름을 Playwright에 추가한다.
  - 잘못된 비밀번호, 정상 로그인, 새로고침 후 세션 유지, 로그아웃을 검증한다.
- [x] 접근성 자동 검사를 CI에 추가한다.
  - 로그인과 주요 화면에서 label, 명암, 키보드 탐색 및 ARIA 오류를 검사한다.
- [x] 큰 차트 번들의 추가 경량화 가능성을 측정한다.
  - 현재 번들 크기를 기준선으로 기록하고 변경 시 크기 회귀를 CI에서 감지한다.
- [x] 공통 API 오류·로딩·빈 상태 컴포넌트를 추출한다.
  - 화면별로 중복된 메시지와 재시도 UI를 일관된 컴포넌트로 통합한다.

### P3 — 분석 및 편의 기능

- [x] 포트폴리오 기간별 손익과 실현·미실현 손익 추이를 제공한다.
- [x] 거래 내역을 CSV로 내보내고 다시 가져오는 기능을 추가한다.
- [x] 관심 종목에 메모와 사용자 지정 그룹을 추가한다.
- [x] 차트 설정과 최근 조회 조건을 브라우저에 저장하고 초기화 기능을 제공한다.

## 다음 개선 로드맵

최근 추가한 통합 검색, 시장 순위 목록, 차트 전용 탭, 계좌 기반 포트폴리오를 실제 개발 서버에서 안정적으로 사용할 수 있게 만드는 후속 작업이다.

### P0 — 실 API 연동 안정화

- [x] 계좌 API의 실제 응답 계약을 개발 키로 검증한다.
  - `ka00001`, `kt00018`의 운영·모의투자 응답 필드와 오류 코드를 각각 확인한다.
  - 보유 종목 없음, 복수 계좌, 휴면·해지 계좌, 장 마감 상태를 처리한다.
  - `cont-yn`, `next-key`가 반환되는 계좌의 보유 종목을 끝까지 조회한다.
- [x] 시장 순위 API의 실제 응답 계약을 개발 키로 검증한다.
  - 급등·급락·거래량·거래대금·신고가·신저가 응답의 종목 코드와 숫자 형식을 고정한다.
  - 일부 순위 API만 실패해도 성공한 목록은 계속 표시하도록 부분 실패를 격리한다.
  - 장 마감, 데이터 없음, API 호출 제한 상태에 맞는 안내 문구를 제공한다.
- [x] 키움 API 호출량을 관리한다.
  - 순위 카드 동시 호출 수, 재시도 횟수, 백오프, 캐시 만료 시간을 API 제한에 맞게 조정한다.
  - 수동 새로고침의 연속 클릭을 방지하고 다음 갱신 가능 시점을 표시한다.
- [x] 계좌번호와 민감 정보를 보호한다.
  - 화면에는 계좌번호 일부만 마스킹하고 서버 로그와 오류 응답에는 전체 계좌번호를 남기지 않는다.
  - API 키, 시크릿 키, 토큰이 프론트 응답이나 추적 로그에 포함되지 않는지 회귀 테스트를 추가한다.

### P1 — 화면 흐름과 데이터 품질

- [x] 검색 결과와 차트 선택 상태를 URL에 동기화한다.
  - `/chart?code=005930`처럼 직접 접근, 새로고침, 뒤로 가기 후에도 선택 종목을 복원한다.
  - 검색 목록, 시장 순위, 관심 종목, 계좌 보유 종목에서 동일한 차트 이동 규칙을 사용한다.
- [x] 종목 검색 분류 정확도를 높인다.
  - 종목명 문자열 추정 대신 시장 구분, ETF/ETN, 업종 등 공식 메타데이터를 사용한다.
  - 초성·부분 일치·종목 코드·시장·상품 유형을 조합한 검색과 정렬 기준을 문서화한다.
- [x] 계좌 기반 포트폴리오 사용성을 개선한다.
  - 복수 계좌 선택, 보유 종목 정렬·필터, 손익·평가액 합계 검산, 마지막 갱신 시각을 지원한다.
  - 보유 비중 차트와 종목별 수익 기여도를 추가한다.
  - 계좌 미개설, 잔고 없음, 인증 만료를 서로 다른 빈 화면으로 안내한다.
- [x] 레거시 수동 포트폴리오의 처리 방침을 확정한다.
  - 기존 `/api/portfolios`와 관련 테이블을 유지·숨김·제거 중 하나로 결정한다.
  - 제거 시 기존 로컬 데이터를 백업하거나 내보낼 수 있는 마이그레이션 절차를 제공한다.
  - 알림·테스트·문서에서 수동 포트폴리오 의존성을 함께 정리한다.
  - **결정**: 숨김 처리 — UI(`/portfolio` 탭)는 계좌 기반 `AccountPortfolio`로 전환 완료. `PortfolioController`·`PortfolioService`·DB 테이블은 기존 사용자 데이터 보호를 위해 유지. 프론트 API 함수는 정의만 존재하고 호출 없음.
- [x] 데이터 신선도를 명확히 표시한다.
  - 실시간, 지연, 장 마감 데이터를 구분하고 조회 기준 시각을 각 카드와 차트에 표시한다.
  - 오래된 캐시 데이터와 API 장애 시 대체 데이터를 시각적으로 구분한다.

### P2 — 테스트와 관측성

- [x] 브라우저 E2E 시나리오를 현재 화면 구조에 맞게 갱신한다.
  - 통합 검색 → 차트, 시장 순위 → 차트, 계좌 보유 종목 → 차트 흐름을 검증한다.
  - 기존 수동 포트폴리오 등록 중심 테스트를 계좌 조회 중심 시나리오로 교체한다.
  - 직접 URL 접근, 새로고침, 뒤로 가기, 모바일 화면을 추가한다.
- [x] 키움 실응답 기반 계약 픽스처를 확장한다.
  - 개인정보를 제거한 계좌·순위 성공/실패/빈 응답 샘플을 테스트 리소스로 관리한다.
  - 키움 필드 추가·변경 시 DTO 매핑 실패를 즉시 발견하도록 계약 테스트를 추가한다.
- [x] 장애 원인을 화면과 로그에서 빠르게 구분한다.
  - 인증 실패, 호출 제한, 키움 원격 오류, DB 오류, 프론트 프록시 오류에 서로 다른 오류 코드를 부여한다.
  - `traceId`, 키움 API ID, 응답 시간, 캐시 적중 여부를 구조화 로그와 메트릭에 남긴다.
  - 순위 카드별 부분 실패와 계좌 조회 실패를 별도 지표로 집계한다.
- [x] 표와 차트의 접근성·반응형 동작을 보강한다.
  - 키보드로 행을 선택할 수 있게 하고 포커스 표시와 스크린리더 레이블을 제공한다.
  - 작은 화면의 표 스크롤, 긴 종목명, 큰 금액, 음수 손익 표시를 회귀 테스트한다.

### P3 — 분석 기능 확장

- [x] 시장 탐색 카드를 사용자가 구성할 수 있게 한다.
  - 등락률, 거래량 급증, 거래대금, 신고가·신저가 외에 외국인·기관 수급과 시가총액 기준을 검토한다.
  - 한 탭에서 카드 표시 여부, 순서, 개수, 시장을 저장하고 복원한다.
  - 현재는 응답 계약이 검증된 급등·급락·거래량 카드를 제공하며, 표시 여부·순서·5/10개·KOSPI/KOSDAQ 필터를 브라우저에 저장한다.
  - 거래대금·신고가·신저가·수급·시가총액 카드는 키움 실응답 계약을 확보한 뒤 별도 확장한다.
- [x] 종목 비교 기능을 추가한다.
  - 여러 종목의 정규화 수익률, 거래량, 이동평균을 같은 기간으로 비교한다.
  - 비교 목록을 관심 종목과 검색 결과에서 바로 구성할 수 있게 한다.
- [x] 계좌 분석을 확장한다.
  - 업종·자산 유형별 편중, 종목별 손익 기여도, 현금 비중을 제공한다.
  - 계산 결과가 키움 계좌 합계와 다를 경우 차이와 계산 기준을 표시한다.
- [x] 사용자 설정을 로컬 환경에 저장한다.
  - 선택 계좌, 검색 필터, 시장 카드 구성, 차트 기간을 브라우저에 저장한다.
  - 초기화와 내보내기 기능을 제공하고 민감 데이터는 저장하지 않는다.
  - 단일 계좌 API이므로 선택 계좌는 계좌번호 대신 `primary` 식별자만 저장한다. 업종은 잔고 API에서 제공될 때 세분화하며 현재는 미분류로 명시한다.
